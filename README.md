# Personal Bash Scripts

This repo contains my personal daily-use Bash scripts.

## Nginx Setup (Ubuntu VM)
`ngnix setup.sh` sets up Nginx on a new Ubuntu VM.

Run:
```bash
sudo bash "ngnix setup.sh"
```
